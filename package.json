{
  "name": "gd-store",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,magenta \"npm run dev --prefix backend\" \"npm run dev --prefix frontend/GdStore\"",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend/GdStore"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "vite": "^8.0.16"
  },
  "dependencies": {
    "react-hot-toast": "^2.6.0",
    "stripe": "^22.1.1"
  }
}
